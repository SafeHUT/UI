import 'package:flutter/material.dart';

class VpnScreen extends StatelessWidget{
  VpnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('in progrss vpn...'),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class BrowseScreen extends StatelessWidget{
  BrowseScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Center(
        child: Text('hello browser'),
       ), 
    );
  }
}